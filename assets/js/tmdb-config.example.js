// Copy this file to tmdb-config.js (which is gitignored — never commit your
// real key, per the same rule Config.swift already follows in the app repo)
// and fill in your TMDB v3 API key: https://www.themoviedb.org/settings/api
window.TMDB_API_KEY = "YOUR_TMDB_API_KEY";
